import 'package:flutter/material.dart';

class ShoppingCartContent extends StatelessWidget {
  const ShoppingCartContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "This is shopping cart view",
      ),
    );
  }
}
